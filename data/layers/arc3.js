// Arc III of the Passage. Contact, Epoch, Resource, Stakes, in order.
// Stakes carries the three-sentence manifesto and the closing question; it is
// the final layer, so its closing card yields the done state.
import contact from "./08-contact.js";
import epoch from "./09-epoch.js";
import resource from "./10-resource.js";
import stakes from "./11-stakes.js";

export const arc3 = [contact, epoch, resource, stakes];
