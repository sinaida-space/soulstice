// Journal / Lens / Statement / Return route registration (milestone M6).
// M6 fills secondaryRoutes with entries shaped { id, enter }.
// Only M6 edits this file.

// import { journalMode, lensMode, statementMode, returnMode } from "./secondary.js";

export const secondaryRoutes = [
  // journalMode, lensMode, statementMode, returnMode
];
